import osfrom typing import Optionalfrom pypdf import PdfReaderfrom docx import Documentclass ResumeExtractor:    @staticmethod    def extract_text_from_file(file_path: str) -> str:        """Extract text from PDF or DOCX file"""        file_ext = os.path.splitext(file_path)[1].lower()                if file_ext == '.pdf':            return ResumeExtractor._extract_from_pdf(file_path)        elif file_ext in ['.docx', '.doc']:            return ResumeExtractor._extract_from_docx(file_path)        else:            raise ValueError(f"Unsupported file format: {file_ext}")        @staticmethod    def _extract_from_pdf(file_path: str) -> str:        """Extract text from PDF file"""        text = ""        try:            with open(file_path, 'rb') as pdf_file:                pdf_reader = PdfReader(pdf_file)                for page in pdf_reader.pages:                    text += page.extract_text()        except Exception as e:            raise Exception(f"Error extracting PDF: {str(e)}")        return text        @staticmethod    def _extract_from_docx(file_path: str) -> str:        """Extract text from DOCX file"""        text = ""        try:            doc = Document(file_path)            for paragraph in doc.paragraphs:                text += paragraph.text + "\n"        except Exception as e:            raise Exception(f"Error extracting DOCX: {str(e)}")        return text